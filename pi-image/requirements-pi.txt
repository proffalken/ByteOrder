# Pi-specific requirements for the ByteOrder print service.
#
# Differences from print-service/requirements.txt:
#   - psycopg2-binary omitted: installed from apt (python3-psycopg2) to avoid
#     compiling a C extension inside the ARMv6 QEMU chroot.
#   - OpenTelemetry omitted: optional at runtime (only activates when
#     OTEL_ENDPOINT env var is set) and grpcio has no ARMv6 wheel.
#   - pytest omitted: not needed on the device.

redis==5.1.0
requests==2.32.4
sqlalchemy==2.0.35
pydantic-settings==2.5.2
